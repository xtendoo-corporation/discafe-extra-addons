<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="view_stock_move_line_detailed_operation_tree_internal_lot" model="ir.ui.view">
        <field name="name">stock.move.line.operations.list.internal.lot</field>
        <field name="model">stock.move.line</field>
        <field name="inherit_id" ref="stock.view_stock_move_line_detailed_operation_tree" />
        <field name="arch" type="xml">
            <field name="lot_id" position="attributes">
                <attribute name="column_invisible">context.get('picking_code') not in ('incoming', 'internal') or context.get('show_lots_text') or (context.get('picking_code') == 'internal' and not picking_type_use_existing_lots)</attribute>
            </field>
            <field name="lot_name" position="attributes">
                <attribute name="column_invisible">context.get('picking_code') not in ('incoming', 'internal') or not context.get('show_lots_text')</attribute>
            </field>
        </field>
    </record>
</odoo>
